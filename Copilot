Continue on the AVIT6799379 branch (bugfix/RD-10887-AVIT6799379-debug-code-auth) 
in salucro.com. I want to complete everything that CAN be fixed in code, and 
leave only the infrastructure work for a child ticket.

VERIFY FIRST — do not proceed past this until you answer:

1. You added $login to v2.1/convert_scan_image.php. Did you actually verify 
   nothing calls this endpoint anonymously? Show me:
   - Every caller of convert_scan_image.php across the repo (forms, JS/AJAX, 
     links, config)
   - Whether any of them run in an unauthenticated context
   - The exact $login value you set, and whether it matches the convention 
     used in v2.1/payments.php line 7
   If you have NOT verified this, do it now and report before continuing. 
   Adding auth to an endpoint that legitimately serves anonymous callers 
   would break the scanner flow in production.

2. Show me `git status` and `git diff` on this branch so I can see exactly 
   what's currently changed and which commits exist.

THEN IMPLEMENT (after I confirm step 1 is clean):

3. Add the isImageFormatValid() gate before BOTH convertCheckImage() calls 
   in v2.1/convert_scan_image.php, matching the sibling-caller convention. 
   Show me the sibling implementation you're matching — do not invent a new 
   pattern.

4. Tell me whether v2.1/payments.php (reads $_POST['frontCheckImageBase64'] 
   at line 301, calls convertCheckImage() at 2498-2499 with no format ga
