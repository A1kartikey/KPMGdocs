For RD-9347, now implement the fixes in
refund_returned.php ONLY.

Make these changes strictly:

1. Before executing refund_returned(ticketid) at line 19:
   - Add server-side check: verify current logged-in 
     user is authorized for this ticketid
   - Reject with error if not authorized

2. Before lookup queries at lines 116 and 120:
   - Validate AccountNumber belongs to current user

3. Convert ALL SQL concatenation to prepared statements
   at lines 85,86,90,91,96,97,111,112,116,120

4. Validate and sanitize all GET/POST inputs:
   AccountNumber, Amount, TxnDate, TimeHh, TimeMm,
   TimeSs, TransactionId

Follow existing patterns in this codebase only.
Change ONLY refund_returned.php.
Show me the diff after changes.
