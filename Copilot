For RD-9347 in refund_returned.php,
address these two reviewer comments:

1. In validateAccountNumber function:
   Remove ONLY the preg_match regex condition.
   Keep: empty check and strlen > 64 check.
   Reason: account format is configurable,
   no additional format condition needed.

2. Remove the entire block that queries
   u.username, u.status, up.Status
   at lines 233-236.
   Reason: only support users access this page,
   this extra permission check is not needed.

Do NOT change anything else.
Show diff after changes.
