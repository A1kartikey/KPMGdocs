For RD-9347, fix ONLY these SQL issues 
in refund_returned.php:

1. Line 58: Remove redundant GROUP BY since
   DISTINCT already ensures uniqueness.
   Change: SELECT DISTINCT up.EntityId ... GROUP BY up.EntityId
   To: SELECT DISTINCT up.EntityId ... (no GROUP BY)

2. Lines 300, 320: Replace SELECT * with only
   the specific columns actually used in the
   display loop at lines 410-413.
   Read what columns are used then select only those.

Do NOT change anything else.
Show me the diff after changes.

For RD-9347, fix ONLY the $entityCsv string
concatenation in refund_returned.php at
lines 165, 272, 282, 292, 327.

Current pattern uses makeIntCsv() which is
intval() protected but still concatenated.

Convert to use quoteIntoMany() with bound
parameters following the same pattern already
used elsewhere in this file.

Do NOT change anything else.
Show me the diff after changes.


For RD-9347, do a final check of refund_returned.php:

1. Confirm GROUP BY fix is correct
2. Confirm SELECT * is replaced correctly
3. Confirm $entityCsv concatenation is fixed
4. Confirm no new issues introduced
5. Confirm all original security fixes still intact

Report only. Do NOT change anything.
Give final production ready verdict.
