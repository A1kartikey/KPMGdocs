For RD-9347, review refund_returned.php only.
Check ONLY these two things:

1. Are all ticket requirements implemented:
   - Authorization before refund_returned(ticketid)
   - AccountNumber ownership validation
   - All SQL uses prepared statements
   - All GET/POST inputs validated/sanitized

2. Any duplicates:
   - Duplicate functions
   - Duplicate validation logic
   - Redundant queries

Report findings only. Do NOT change anything.


For RD-9347, review ONLY the SQL queries
in refund_returned.php:

- Any string concatenation still remaining?
- All queries use prepared statements + bind_param?
- No SELECT * unless necessary?
- No unnecessary joins or subqueries?
- Queries optimized for production performance?

Report issues only. Do NOT change anything.


For RD-9347, compare refund_returned.php against
other existing PHP files in this codebase:

1. Same DB connection pattern?
2. Same prepared statement pattern?
3. Same session/auth pattern?
4. Same error handling style?
5. No raw user input reaching any query?
6. Error messages safe - no DB details exposed?
7. Auth failures exit cleanly?

Report any mismatches. Do NOT change anything.
Confirm if production ready.
