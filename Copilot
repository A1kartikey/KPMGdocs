For RD-9705, confirm that 92.5% is the true maximum 
achievable without touching non-test files.

For every function below 100%, specifically:
- unwrapInputData (75%)
- getConfigByIndex (90%)
- MakeTransactionNotificationActivity (87.2%)
- NotifyEntityProfileDetailsActivity (86%)
- InitializeCheckImageRequestAdapter (87.5%)

For each one:
1. Show me exactly which lines/branches are NOT covered
2. Explain WHY they cannot be covered without 
   changing non-test files
3. Confirm if it's due to:
   - Unreachable code paths
   - Hard-coded dependencies
   - Marshal/unmarshal errors that cannot be injected
   - External service calls that cannot be mocked

Give me a clear YES or NO for each function:
"Can this be improved without touching non-test files?"
