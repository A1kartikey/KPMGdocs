# Task: check out RD-11688 and get it running against my local Datadog agent

Branch: feature/RD-11688-integ-auth-api-v1/integ/auth
Repo: Salucro/PartnerIntegrations/POG

My previous run analysed the wrong checkout. Start by confirming which branch is
actually active, then switch.

Goal for THIS turn: the service starts and stays up. Nothing else. Do not refactor,
do not fix findings, do not review code quality.

## Step 1 — Check out the branch
  git status
  git stash list
Report anything uncommitted before you switch — do not discard my work.
Then:
  git fetch origin
  git checkout feature/RD-11688-integ-auth-api-v1/integ/auth
  git log --oneline -3
Confirm HEAD is at f51728e. Report the actual output.

## Step 2 — Build
  go build ./...
If it fails, show the full error and stop. Tell me what is missing (dependency,
generated code, go version) and the command to fix it — do not fix it yourself.

## Step 3 — Work out how to start it
Read, do not guess:
  - the main entrypoint (cmd/ or app/cmd/)
  - app/src/config/ for every env var the service requires to boot, not just DD_*
  - any Makefile, docker-compose.yml, .env.example, or README run instructions in the repo
Report: the exact run command, and the complete list of required env vars with the
value each one needs. Mark which ones I must supply (DB, secrets, upstream URLs) versus
which have working defaults.

## Step 4 — Confirm my Datadog agent
  docker ps
Identify the Datadog agent container, its name, and which ports it publishes.
Confirm the DogStatsD UDP port (normally 8125) is actually mapped to the host.
If DogStatsD is not exposed, say so and give me the docker run/compose change needed.

## Step 5 — Start it
Give me one copy-pasteable command with all env vars inline, pointing DD_AGENT_HOST
and the DogStatsD port at my container. Use PowerShell syntax — I am on Windows/pwsh.
Run it. Report the startup log verbatim.

## Step 6 — Confirm it is up
Tell me how to prove the process is serving: the port it listens on, and a curl or
Invoke-WebRequest against a health endpoint (find the real path in the routes, do not
assume /health). Run it and show the response.

## Report
Exactly two things:
  1. RUNNING: yes/no
  2. If no: the single blocking error, verbatim, and what I need to supply to clear it

Do not analyse metric names, tags, or correctness in this turn.
