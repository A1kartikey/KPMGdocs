RD-10887, finding AVIT6799405 (CWE-502) in the salucro.com PHP repo. I want 
to fix the deserialization sink in epic/payment.php. Investigate repo-wide 
FIRST, then we decide the fix together. Do not change code until I confirm 
the approach.

PHASE 1 — SCOPE (read-only, report findings):

1. clientAuth: grep the ENTIRE repo (not just payment.php) for "clientAuth". 
   Show every hit with file:line. Specifically: is clientAuth ever WRITTEN 
   into the session payload anywhere, and is it ever READ downstream? Also 
   check git history — was it populated in the past? A previously-rendered 
   browser form could still POST the old shape during deployment.

2. encrypt_decrypt(): find where it's DEFINED. Is it local to payment.php or 
   a shared helper (site_inc.php or another common include)? List every 
   caller across the repo. This decides whether the hardcoded key / static 
   IV is a payment.php problem or a repo-wide one.

3. The encrypted-hidden-field pattern: do other pages emit an encrypted blob 
   to the browser and accept it back? Grep for "sessiondata" and for hidden 
   inputs carrying encrypted state. Tell me if this is unique to payment.php 
   or systemic.

4. Other CWE-502 sinks: grep for unserialize() anywhere it touches request 
   data — $_POST, $_GET, cookies, or anything decrypted from a client blob. 
   List them with file:line.

5. Is the secret key committed in source and present in git history? Do NOT 
   print the key value — just tell me yes/no and where it's defined.

PHASE 2 — RECOMMENDATION (still no code changes):

Based on Phase 1, tell me which of these applies, with evidence:
 (a) NARROW — clientAuth is genuinely dead repo-wide: delete the 
     unserialize call. One file, no behavior change.
 (b) MEDIUM — clientAuth IS consumed: replace unserialize with 
     json_decode($x, true) plus an allow-list of expected keys, so no PHP 
     object instantiation is possible. Show me exactly what consumes it and 
     what the allow-list should contain.
 (c) WIDE — the crypto weakness (hardcoded key, static IV, no integrity 
     check) needs fixing too. Flag this as likely a SEPARATE ticket rather 
     than folding it into this PR.

RULES:
- Do not "sanitize the input" as a fix for unserialize — that does not work.
- If the scan's description doesn't match what's actually in the code, say so.
- Do not modify anything until I confirm which option to take.
- Note: php -l can't run in this environment (PHP not on PATH), so tell me 
  what I need to lint elsewhere before this can go in a PR.
