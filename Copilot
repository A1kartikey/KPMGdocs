For RD-9937, do ONLY these two changes:

1. chihuahua.go line 702:
   Remove from registerWorkflows ONLY:
   - openBalanceWorkflow.SendPaymentLink
   - openBalanceWorkflow.OnetimeOptIn
   Do NOT remove other workflow registrations.

2. workflow.go line 1343:
   Remove these two functions entirely:
   - func (bnwf *Workflow) SendPaymentLink(...)
   - func (bnwf *Workflow) OnetimeOptIn(...)

Do not touch any other files.
Show diff after changes.
Confirm build passes:
go build ./services/chihuahua/...


For RD-9937, before proceeding check:

1. Search for any remaining references:
   grep -r "send-payment-link-wf-migration" ./services/chihuahua/
   grep -r "SendPaymentLinkWfMigration" ./services/chihuahua/
   grep -r "SendPaymentLink" ./services/chihuahua/service/natshandler/
   grep -r "OnetimeOptIn" ./services/chihuahua/

2. Check if openBalanceWorkflow.SendPaymentLink
   and OnetimeOptIn still exist in:
   - chihuahua.go
   - workflow.go

3. Check SFN poller argument order is correct
   in all adapter files

Report exactly what is still remaining
before I decide next steps.
