On branch bugfix/RD-10887-AVIT6799380-paypal-transaction-details 
(salucro.com), implement ONLY the change that is safe without the pending 
auth design decision. I run all git commands myself.

DO:
1. Delete both print_r() debug outputs in xadmin/master/GetTransactionDetails.php 
   (including the final print_r($_xml)). Show me each one in context first, 
   and confirm nothing parses that output — check whether any caller scrapes 
   the response body rather than using the JSON.
2. Show me the diff before I commit.

DO NOT (blocked on design decision):
- Add $login or any auth guard — paypal_transactions.php:72 calls this 
  server-to-server with curl, no session; a login gate would break the 
  PayPal Here import.
- Narrow or remove the return=1 response — that is the exact shape the 
  importer consumes.
- Change SOAP request construction to DOMDocument — it alters the PayPal 
  wire format and needs integration testing alongside the auth change.
- Touch credentials — rotation is an ops task, not a code change.

THEN:
3. Summarize precisely what this PR does and does not address, for the PR 
   description.
4. Draft a short, factual statement of the design question for my seniors: 
   the three options (shared service, service-to-service credentials, 
   retire the importer) with the trade-off of each. Do not recommend one.
5. Commit message must contain the standalone token _SCAN_.

Do not commit or push. PHP is not on my PATH — tell me what needs linting 
in CI.
